[pytest]
DJANGO_SETTINGS_MODULE = core.settings
python_files = tests.py test_*.py *_tests.py
# Les tests tournent sur SQLite par défaut (aucune DATABASE_URL fournie),
# ce qui les rend autonomes et rapides.
addopts = -ra -q
